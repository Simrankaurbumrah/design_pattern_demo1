/**
* notifications.groovy
* Notifications helpers (email, slack, etc.)
*/
 
def sendEmail(String to, String subject, String body) {
    // Placeholder for sending email
    echo "Sending email to ${to}: ${subject}"
}
